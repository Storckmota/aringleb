{
  "name": "aringleb-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check:site": "node scripts/site-check.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test scripts/insights/*.test.mjs",
    "build:fixtures": "node scripts/insights/build-fixtures.mjs"
  },
  "devDependencies": {
    "vite": "^8.1.2"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
